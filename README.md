# WebCrawler
This is a simple webcrawler program that writes the HTML of each page found to the pages directory. The crawler starts at a given URL and explores each link in the given pages. The program continues until it reaches the desired depth.
